import React from 'react';
import styles from '../css/styles.module.css';

const Footer: React.FC = () => {
  return <footer className={styles.footer}>created by ali Hosseini</footer>;
};

export default Footer;
